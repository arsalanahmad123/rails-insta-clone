<%= form_for user,url: update_bio_path,method: :patch  do |u| %>
  <%= u.text_field :bio,class: "bg-gray-100 text-[14px] p-2 rounded",placeholder: "Add Bio",required: true %>
  <%= u.submit "Save",class: "bg-gray-200 text-[14px] py-2 px-3 rounded cursor-pointer " %>
<% end %>
